const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../header';

describe('core.actions.header', () => {
  it('should do something');
});
