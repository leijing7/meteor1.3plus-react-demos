const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../notice';

describe('admin.actions.notice', () => {
  it('should do something');
});
